{
  "id": "c8f7a1b2-3d4e-5f6a-7b8c-9d0e1f2a3b4c",
  "name": "Agent Orchestra - Scheduler",
  "active": false,
  "nodes": [
    {
      "id": "n1-schedule-trigger",
      "name": "Schedule Trigger",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.3,
      "position": [250, 300],
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "minutes",
              "minutesInterval": 15
            }
          ]
        }
      }
    },
    {
      "id": "n2-check-queue",
      "name": "Check Queue",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [450, 300],
      "parameters": {
        "jsCode": "const https = require('https');\nconst url = process.env.SUPABASE_URL;\nconst key = process.env.SUPABASE_SERVICE_KEY;\n\nif (!url || !key) {\n  throw new Error('SUPABASE_URL e SUPABASE_SERVICE_KEY s\u00e3o obrigat\u00f3rios');\n}\n\nfunction supabaseGet(path) {\n  return new Promise((resolve, reject) => {\n    const u = new URL(path, url);\n    const opt = {\n      hostname: u.hostname,\n      path: u.pathname + u.search,\n      method: 'GET',\n      headers: {\n        apikey: key,\n        Authorization: 'Bearer ' + key,\n      },\n    };\n    const req = https.request(opt, res => {\n      let data = '';\n      res.on('data', c => data += c);\n      res.on('end', () => {\n        try { resolve(JSON.parse(data)); }\n        catch { reject(new Error('Falha ao parsear resposta: ' + data)); }\n      });\n    });\n    req.on('error', reject);\n    req.end();\n  });\n}\n\nconst pending = await supabaseGet('/rest/v1/agent_queue?status=eq.pending&order=priority.desc,created_at.asc&limit=5');\nreturn pending.length > 0 ? pending : [{ empty: true, message: 'Fila vazia' }];"
      }
    },
    {
      "id": "n3-dequeue-process",
      "name": "Dequeue & Process",
      "type": "n8n-nodes-base.executeCommand",
      "typeVersion": 1,
      "position": [650, 300],
      "parameters": {
        "command": "python3",
        "parameters": "/workspace/agent-orchestrator.py process"
      }
    },
    {
      "id": "n4-queue-empty",
      "name": "Queue Empty",
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [650, 500],
      "parameters": {}
    }
  ],
  "connections": {
    "n1-schedule-trigger": {
      "main": [
        [{ "node": "n2-check-queue", "type": "main", "index": 0 }]
      ]
    },
    "n2-check-queue": {
      "main": [
        [
          { "node": "n3-dequeue-process", "type": "main", "index": 0 },
          { "node": "n4-queue-empty", "type": "main", "index": 0 }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  },
  "staticData": null,
  "tags": ["agent-orchestra", "scheduler"]
}
