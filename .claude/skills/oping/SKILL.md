---
name: oping
description: Test connectivity with OpenCode (shorthand: oc) via the `oping` CLI. Use when the user explicitly asks to check OpenCode/oc status/connection.
---

# oping

Test connectivity with OpenCode via the `oping` CLI.

## Usage

```bash
oping
```

Checks if OpenCode is running and responsive. Returns connection status and latency.
